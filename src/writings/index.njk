---
layout: main
title: writings
---

<h1>Writings</h1>

<ul>
  {% for article in collections.writings %}
    <li>
      <span class="mono-date">{{ article.date.toLocaleDateString() }}</span>
      <a href="{{ article.url }}">{{ article.data.title }}</a>

      <div class="tag-list">
        {% for tag in article.data.tags %}
          {% if tag !== 'writings' %}
            <div class="tag-list__tag">{{ tag }}</div>
          {% endif %}
        {% endfor %}
      </div>
    </li>
  {% endfor %}
</ul>
